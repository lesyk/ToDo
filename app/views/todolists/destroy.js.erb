$('[class^=calendar current]').attr("class", "calendar current").fadeOut();
$('.preview-pane .preview').animate({left: "-375px"}, 300);
$('.preview-pane .preview').html('');
$('.list-view li').removeClass('current');